# 슬라이드 덱 시각 작업 인수인계

> 작성: 2026-09-16 23:40. 다음 세션이 이 파일만 읽고 이어서 작업할 수 있도록 쓴 문서다.
> **작업 시작 전에 이 문서를 처음부터 끝까지 읽을 것.**

## 0. 지금 할 일 (한 줄)

**`live-commerce-deck.html` 103장 전체에 §4의 시각 시스템 9개를 적용한다.** 단 §5의 사용자 피드백 2건을 반드시 반영할 것.

## 1. 대상 파일

| 파일 | 설명 |
|---|---|
| `live-commerce-deck.html` | **작업 대상.** 발표 슬라이드 덱, 103장, 단일 HTML(인라인 `<style>` + `<script>`) |
| `deck-sample.html` | 대표 4장 샘플. 사용자가 브라우저로 보고 피드백을 준 파일. **단 이 문서를 쓰는 시점에 아직 수정 중이었으므로, 샘플 파일과 이 문서가 어긋나면 언제나 이 문서(§4·§5)가 우선이다.** 특히 샘플에 `hero-invert`(흰 배경)가 남아 있어도 §5-1에 따라 쓰지 않는다. 저장소에 없으면 없는 대로 진행하면 된다 — 이 문서만으로 작업이 가능하게 써 두었다 |
| `교안-상세.md` | 강사용 상세 교안. 이번 시각 작업에서는 **건드리지 않는다** |
| `hyundai-livecommerce-seminar-curriculum.md` | 커리큘럼 개요. 이번 작업에서는 **건드리지 않는다** |

## 2. 이 덱이 뭔지

- 현대홈쇼핑 **K-뉴딜 아카데미 2기** 라이브커머스 과정의 **오프라인 집합교육** 발표 덱.
- 수강생 30명(6명×5조), 만 15~34세 미취업 청년, 쇼호스트·라이브커머스PD·크리에이터 지망생.
- 강사가 **본인 노트북을 프로젝터에 연결해 라이브로 직접 넘기며** 진행한다. 하루 7교시(9시~18시).
- 세미나 실시일: 2026-09-18.

## 3. 현재 덱의 시각 시스템

- 다크 테마. CSS 변수:
  `--bg:#0B0904` `--panel:#171208` `--panel-2:#1F1810` `--ink:#F6EFDD` `--ink-soft:#B7AC90`
  `--ink-faint:#9C9077` `--accent:#FF6B3D`(오렌지) `--accent-soft:rgba(255,107,61,.14)`
  `--gold:#E8B54E` `--line:#332A17` `--danger:#FF4D4D`
- 폰트: `Pretendard Variable`(로컬 `_PretendardVariable.woff2`), 숫자·라벨 일부 `JetBrains Mono`, 일부 `Noto Serif KR`.
  **사용자가 본인 노트북을 가져가 그대로 연결하므로 폰트 로딩 문제는 없다. 폰트 관련 작업은 하지 말 것.**
- 슬라이드 동작: `.slide{position:absolute;inset:0;opacity:0;visibility:hidden}` → `.slide.active`로 표시.
  JS `go(i)`가 `active` 토글 + dots/HUD 갱신 + `sessionStorage`에 현재 인덱스 저장.
- 슬라이드 타입별 클래스:
  `.kw-eyebrow`/`.kw-text`(키워드형), `.tslide-title`/`.tslide-table`(표), `.steps3`/`.step3-card`(3단계 카드),
  `.rcard`/`.role-cards-dark`(역할카드), `.stat-row`/`.stat-box .num`(숫자), `.tl-strip`/`.tl-chip`(타임라인),
  `.list-title`/`.list-items`(목록), `.closing`(테두리 강조), `.prompt-title`/`.prompt-box`(AI 프롬프트),
  `.photo-row`/`.ps-caption`(사진), `.career-timeline`/`.career-seg`, `.source-note`, `.mission-sub`,
  `.eyebrow`/`.s-period`/`.s-title`/`.s-meta`(교시 타이틀), `.bridge-*`(교시 전환)
- 크롬: `.hud`(좌상단 교시·번호), `.dots`(하단), `.navbtn`, `.fsbtn`
- 파일 상단에 `@media (prefers-reduced-motion: reduce)` 블록이 이미 있다. 유지하고 새 애니메이션도 여기서 꺼지게 할 것.

## 4. 적용할 시각 시스템 9개

1. **순차 등장(스태거)** — 리스트 `li`, 표 `tbody tr`, 카드(`.step3-card` `.rcard` `.tl-chip` `.stat-box`)가 슬라이드 진입 시 0.05~0.07초 간격으로 아래에서 올라온다. **기존 마크업을 고치지 않고 CSS `nth-child` 딜레이만으로** 동작해야 한다(103장을 일일이 안 건드리려는 목적).
2. **빌드 모드** — `data-build` 슬라이드는 스페이스/→ 를 누를 때마다 항목이 하나씩 공개되고, 전부 공개된 뒤 한 번 더 누르면 다음 슬라이드로. ← 로 되돌릴 때 단계가 역순으로 정확히 복원돼야 한다. **§5-2의 선별 기준을 반드시 따를 것.**
3. **숫자 카운트업** — `.stat-box .num`이 0에서 목표값까지 차오른다. `10.3%` `69.2%` `3.75초` `1,000` `700` 처럼 소수점·쉼표·단위가 섞여 있으니 **숫자 부분만 보간하고 접두/접미 문자는 보존**할 것. 슬라이드 재방문 시 재생.
4. **교시별 색 아이덴티티** — `data-period="1"~"7"`에 따라 액센트가 오렌지→앰버→코랄 범위 안에서 이동. **색상환을 좁게 쓸 것 — 알록달록해지면 실패다.** 교시 타이틀 슬라이드에서만 강하게, 본문은 은은하게.
5. **리듬 슬라이드** — 어두운 화면이 길게 이어지니 "이 장은 다르다"가 필요하다. **§5-1 참조 — 흰 배경은 금지.**
6. **진행 바** — 하단 dots 103개가 뭉개진다. **교시별 세그먼트 바**로 교체. 1~7교시 구간이 보이고 현재 교시 안에서 진행도가 차오르는 형태.
7. **배경 질감** — 균일 단색 탈피. 중앙 방사형 광원 + 아주 미세한 노이즈. **프로젝터에서 그라디언트 밴딩이 보이면 실패**이니 약하게, 교시 색과 연동.
8. **표·카드 강조** — `.tslide-table`에서 `td.hi`가 있는 행에 좌측 액센트 바, 등장 시 한 번 반짝. 카드류에 미세한 입체감.
9. **사진 슬라이드** — 느린 줌(Ken Burns) + 캡션이 사진보다 조금 늦게 등장.

## 5. 사용자 피드백 — 반드시 지킬 것

샘플 4장을 브라우저로 확인한 뒤 나온 지시다. 이 두 가지 외에는 **"나머지는 오케이"** 라고 했으니 샘플의 나머지 처리를 그대로 확장하면 된다.

### 5-1. 흰 배경 리듬 슬라이드는 폐기 (위 4번 항목 중 5번)

사용자 원문: **"2교시 2페이지로 만든 그 하얀 화면에 검은색 위에 좀 있는거. 이건 좀 이상하다 나머지는 오케이."**

- 대상은 `class="slide hero-invert"` 로 된 2교시 리캡 슬라이드
  ("몰라서 혼나는 신입은 없다 / 일 잘하는 신입 = 선배를 안 불안하게 하는 신입 / 빠른 초안 > 느린 완벽").
- **순백 배경 + 검은 글씨 방향 자체를 폐기한다.** 이 덱의 어두운 톤과 맞지 않는다.
- 이 슬라이드는 **다크 테마 안에서** 리듬이 살아나게 다시 만든다. 밝기를 뒤집는 대신 교시 액센트 색을 화면 전체에 깔거나, 패널·테두리·타이포 스케일로 차별화하는 식. 방법은 재량이되 **배경을 희게 만들지 말 것.**
- 원본 덱에 `hero-invert`는 이 한 장뿐이다. 전체 적용에서도 흰 배경은 쓰지 않는다.

### 5-2. 빌드 모드는 선별 적용

사용자 원문: **"모든 페이지를 이거 다 누르게 할 필욘 없어. 패스할건 패스해도되."**

- `data-build`는 **가려야 의미가 있는 슬라이드에만** 붙인다:
  퀴즈 보기·정답, 채점 배점표, 계산 문제의 정답 — 미리 보이면 김이 새는 것들.
- 그 외 대부분은 **열자마자 전체가 스태거로 다 나타나고, 스페이스 한 번이면 다음 장**으로 넘어간다.
- 이유: 강사가 103장을 넘기는데 장마다 여러 번 눌러야 하면 진행이 끊긴다.
- 실제 후보(내용 확인 후 확정할 것):
  4교시 "즉석 투표 · 손들기 — 마케팅에 가장 가까운 것은?" + "정답은 B",
  3교시 "예제 1 · 정답" / "예제 2 · 정답" / "그래서, 통제할 수 있을까?",
  5교시·6교시 "심사 기준 · 100점" 배점표.
  **이 정도가 상한이다. 더 늘리지 말 것.**

## 6. 작업 제약

- **프로젝터 기준.** 미세한 그림자·저대비 효과·얇은 선은 강의장에서 안 보인다. 대비와 크기로 승부할 것.
- **강사가 라이브로 넘긴다.** 전환은 빠르게(0.3~0.5초). 되감기·빨리감기해도 상태가 깨지면 안 된다.
  앞으로 갔다 뒤로 왔을 때 **빌드 단계와 카운트업이 정확히 복원되는지 직접 확인**할 것.
- **문구(내용)를 절대 바꾸지 말 것.** 이번 작업은 순수하게 시각적인 것이다.
- **외부 CDN을 새로 추가하지 말 것.** 폰트는 이미 있는 것만 쓴다.
- 저장 후 **태그 균형(`<section>` `<div>` `<table>` `<ul>`)과 JS 문법을 반드시 검증**할 것.
  python/node로 여는 태그·닫는 태그 개수를 세면 된다. 작업 전 개수를 먼저 기록해두고 비교할 것.
- `prefers-reduced-motion: reduce`에서 모든 새 애니메이션이 꺼져야 한다.

## 7. 백업

- 작업 시작 전 `live-commerce-deck.html`을 반드시 복사해 둘 것.
- 이 저장소는 git이다(원격 `thenightpd/hyundai-livecommerce-seminar`, 브랜치 `master`).
  **오늘 내용 수정분이 아직 커밋되지 않은 상태다.** `git stash`나 `git checkout` 류로 날리지 말 것.
- **작업 결과는 반드시 커밋하고 `master`에 푸시할 것.** 이 작업은 사용자가 잠든 사이 클라우드에서 도는 예약 실행이라, 푸시하지 않으면 세션이 끝날 때 결과가 통째로 사라진다. 사용자는 아침에 `git pull`로 받아볼 예정이다.
- 커밋은 **시각 작업만 따로** 한 덩어리로 묶는다. 직전 커밋(내용 보강)이 깨끗한 롤백 지점이 되도록.
- 커밋 메시지 끝에 다음 줄을 넣을 것:
  `Co-Authored-By: Claude <noreply@anthropic.com>`

## 8. 오늘 이 덱에 이미 한 작업 (참고 — 다시 하지 말 것)

내용(문구·구성) 보강은 2026-09-16에 이미 끝났다. 덱은 86장 → 103장이 됐다.
4교시 AI 큐시트 실습 복원, 심사 배점표 앞당김, 5교시 STEP 프롬프트 신설,
3교시 직접 계산 실습 추가, 6교시 발표 4분 재설계, 타임라인·용어 정합, 죽은 타이머 코드 100줄 제거 등.
**이번 작업은 여기에 시각 효과만 얹는 것이다. 내용을 다시 손대지 말 것.**

## 9. 완료 후 보고

- 9개 시스템을 각각 어떻게 구현했는지 (핵심 CSS/JS 기법 한두 줄씩)
- 교시별 색 실제 값
- `data-build`를 최종적으로 붙인 슬라이드 목록과 그 이유
- 검증 결과: 태그 균형 수치(전/후), 되감기 동작 확인 여부
- 사용자가 브라우저로 바로 확인할 수 있게 `open live-commerce-deck.html` 안내

---

# 부록 A. 샘플 작업 완료 보고 (2026-09-16 23:55 추가)

`deck-sample.html` 작업이 **끝났고 §5 피드백 2건이 모두 반영됐다.** 이제 이 샘플은 신뢰할 수 있는 레퍼런스다.
(§1의 "샘플이 아직 수정 중" 단서는 해소됐다. 다만 문서와 샘플이 어긋나면 여전히 이 문서가 우선이다.)

**전체 적용 시 샘플의 CSS/JS를 그대로 이식하면 된다.** 아래는 그때 반드시 알아야 할 것들이다.

## A-1. `data-build`를 붙일 슬라이드 — 확정 6장

기준(미리 보이면 김이 새는 것): ① 퀴즈 보기·정답 ② 채점/배점표 ③ 계산 문제의 단계별 정답.
원본 103장을 실제로 훑어 이 기준에 맞는 것은 **6장뿐**이다. **더 늘리지 말 것.**

1. 4교시 "즉석 투표 · 손들기 — 마케팅에 가장 가까운 것은?" (보기 A~D) — 샘플에 적용됨
2. 3교시 "예제 1 · 정답" (2단계 계산)
3. 3교시 "예제 2 · 정답" (4단계 계산)
4. 3교시 "그래서, 통제할 수 있을까?" (5행 판정표)
5. 5교시 "심사 기준 · 100점" (5행) — 샘플에 적용됨
6. 6교시 "심사 기준 · 100점" (같은 표 재등장)

나머지 97장은 스태거만 걸고 **스페이스 한 번에 다음 장**으로 넘어간다.

## A-2. 이미 잡은 버그 3건 — 다시 밟지 말 것

1. **`.slide.hero-invert{position:relative}` 금지.** base `.slide{position:absolute;inset:0}`를 specificity로 덮어써서 슬라이드 박스가 뷰포트를 못 채우고 콘텐츠 크기로 쪼그라든다. `position`을 재선언하지 말 것.
2. **노이즈 레이어에 `mix-blend-mode:overlay` 금지.** raw feTurbulence와 겹치면 대비가 증폭돼 눈에 띄는 얼룩이 생긴다. feColorMatrix로 RGB를 회색(0.5) 고정 + 알파를 원본의 6%로 눌러 쓸 것.
3. **`border-collapse:collapse` 표에서 빌드 숨김이 안 먹는다.** 셀 경계선이 테이블 레벨에서 그려져 `tr`의 `opacity:0`이 구분선·좌측 액센트 바에 적용되지 않는다(공개 전인데 줄이 다 보임). `[data-build] .tslide-table`에만 `border-collapse:separate`로 **스코프해서** 해결할 것 — 다른 표는 원본대로 collapse 유지.

## A-3. 전체 적용 때 반드시 손볼 것

- **`.slide.hero-invert{background:#FFFFFF;}` 원본 규칙을 삭제할 것.** 샘플에서는 뒤에 오는 다크 재설계 규칙이 덮고 있을 뿐이라, 규칙 순서가 바뀌면 흰 배경이 되살아난다. 덮지 말고 **지워라.**
- **7교시 Q&A 슬라이드의 `data-period`가 "6"으로 돼 있다**(원본의 기존 특이사항). 그대로 두면 6교시 색을 쓴다. 손대지 않아도 되지만 알고는 있을 것.

## A-4. 샘플에서 실기 확인을 못 한 것 — 전체 적용 때 육안 확인

- **사진 Ken Burns**(§4-9): 샘플에 사진 슬라이드가 없었다. `photos/` 20장 중 몇 장을 열어 **확대된 이미지가 `.photo-row`의 18px 갭을 침범해 옆 사진과 겹치는지** 볼 것(scale 1.045로 작게 잡아 위험은 낮다).
- **표 강조의 스태거 모드**(§4-8): 빌드가 아닌 일반 표에서 `tr-hi` 등장 반짝임이 자연스러운지. CSS 문법은 검증됐다.

## A-5. 검증 기준선 (전체 적용 후 이 수치와 맞춰볼 것)

- 원본 `live-commerce-deck.html`: **슬라이드 103장**, 교시별 17 / 19 / 24 / 19 / 15 / 9
- 작업 전후로 태그 균형(`section` `div` `table` `tbody` `tr` `td` `ul` `li`)이 동일해야 한다
- `node --check`로 JS 문법 통과할 것
- 되감기 검증: 빌드 슬라이드를 끝까지 공개 → 다음 장 → ←로 복귀했을 때 **단계가 역순으로 정확히 복원**되는지 직접 확인
